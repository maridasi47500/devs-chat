<%= form_with(model: programminglanguage) do |form| %>
  <% if programminglanguage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(programminglanguage.errors.count, "error") %> prohibited this programminglanguage from being saved:</h2>

      <ul>
        <% programminglanguage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :printres, style: "display: block" %>
    <p>entrez le "%s" là où on insert le code dans une seule ligne (bash)</p>
    <p>p.s.
    Pour enregistrer les guillemets ' et " pour le code python, nous pouvons construire le bloc avec EOF

    <code>
    c=&#96;cat <<EOF
    print(122)
    EOF&#96;
    python -c "$c"
    </code>
    ou pour l'exception avec python:
    <code>
      c=&#96;cat <<EOF 
      try:
        %s
        except Exception as e:
          print(e)
          EOF&#96;
          python3 -c "$c"
    </code>


    </p>
    <%= form.text_area :printres %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
